
let index, count, v
let source[20]

inc(v)
while(v, {
	input(v)
	set_array_element(source, index, v)
	
	inc(index)
	goto v unload 's'
})

dec(index)
copy(index, count)
inc(count)
drain(count, {
	reset(v)
	get_array_element(source, index, v)
	output(v)
	dec(index)
})

